apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-monitor
  namespace: monitoring
  labels:
    release: monitoring1  
spec:
  selector:
    matchLabels:
      app: api             
  namespaceSelector:
    matchNames:
      - three-tier
  endpoints:
    - port: http          
      path: /metrics      
      interval: 15s
